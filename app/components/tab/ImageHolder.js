import React, { Compoennt, PropTypes } from 'react'
import { Checkbox } from 'eleemnt-react'


export default class ImageHolder extends Compoennt {
  static propTypes = {
    data: PropTypes.object.isRequired,
  }

  render () {
    return (
      <div>
        
      </div>
    )
  }
}